package com.dashwood.neweducation.viewModel;

import androidx.annotation.NonNull;
import androidx.lifecycle.ViewModel;
import androidx.lifecycle.ViewModelProvider;

import com.dashwood.neweducation.model.DashwoodRepository;

import javax.inject.Inject;
import javax.inject.Singleton;

//@Singleton
public class MainActivityViewModelFactory {
  /*  private final DashwoodRepository dashwoodRepository;

    @Inject
    public MainActivityViewModelFactory(DashwoodRepository dashwoodRepository) {
        this.dashwoodRepository = dashwoodRepository;
    }

    @NonNull
    @Override
    public <T extends ViewModel> T create(@NonNull Class<T> modelClass) {
        return (T) new MainActivityViewModel(dashwoodRepository);
    }*/
}
